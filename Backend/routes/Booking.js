var express = require("express");
var router = express.Router();

// Load User model
const User = require("../models/Users");

router.put("/booksession/:id", (req,res) => {
    // start time
    // end time
    // id
    
    
});


module.exports = router;